# Published Skills Boundary

- This subtree mixes synced public copies from `C:\Users\chenpeng\.codex\skills\` with repo-only skill directories kept only in this repository.
- If a skill also exists under the local Codex home, edit the local source first and refresh this published copy with `..\..\scripts\sync-public-codex.ps1` instead of maintaining both by hand.
- Edit repo-only skill directories directly in this repository and keep any local eval, reference, or script changes scoped to that skill.
